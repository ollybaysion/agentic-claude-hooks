<!--
category: domain-docs
인덱스: .claude/context-docs.domain.json
저장 경로: .claude/docs/domain/<용어 소문자>.md
필수 슬롯: 용어, 정의, 관련 코드 위치
선택 슬롯: 동작/계산식, 불변조건, 엣지케이스, 배경
키워드 기본값: 용어 소문자 + 영문 동의어/풀네임 (예: "tsum", "total sum") — ASCII 소문자만, 한글 단독 키워드는 매치되지 않음
precision 기본값: 1 (용어가 일반 영단어와 겹치면 0.5 제안)
채우기 규칙: {{...}} 안의 지시대로 채운다. "없으면 삭제" 표시가 있는 줄은 해당 없을 때 통째로 지운다.
이 주석 블록은 문서 생성 시 제거한다.
-->

# {{용어 — 통용 표기 그대로, 예: TSUM}}

{{정의 한 줄 — 이 용어가 가리키는 것. 풀네임이 있으면 병기}}

- 동작/계산: {{계산식이나 판정 규칙 — 없으면 이 줄 삭제}}
- 코드 위치: {{구현이 있는 파일/모듈 경로 — src/... 형태, 여러 개면 쉼표로 나열}}
- 불변조건: {{항상 성립해야 하는 것 — 없으면 이 줄 삭제}}

---

## 엣지케이스

{{선택 — 예외 상황과 처리 방식. 여기부터는 1200자 컷 아래라 잘려도 무방}}

## 배경

{{선택 — 왜 이렇게 설계됐는지, 관련 이슈/결정 기록}}
