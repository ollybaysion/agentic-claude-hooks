<!--
category: db-schema
인덱스: .claude/context-docs.db-schema.json
저장 경로: docs/db/<테이블명 소문자>.md
필수 슬롯: 테이블명, 용도, 컬럼표, PK
선택 슬롯: 보조 인덱스, FK/관계, 대표 쿼리, 마이그레이션 주의
키워드 기본값: 테이블명 소문자 — 스키마 접두가 있으면 접두 포함/미포함 둘 다 (예: "fab.lot_history", "lot_history")
precision 기본값: 1 (컬럼이 아주 많은 대형 테이블이면 0.5 제안)
채우기 규칙: {{...}} 안의 지시대로 채운다. "없으면 삭제" 표시가 있는 줄은 해당 없을 때 통째로 지운다.
이 주석 블록은 문서 생성 시 제거한다.
-->

# {{테이블명 — 스키마 접두 포함, 예: FAB.LOT_HISTORY}}

{{용도 한 줄 — 무엇을 저장하는가}}. {{쓰기 주체 → 읽기 주체 — 예: "설비 이벤트 핸들러가 쓰고 리포트 배치가 읽는다"}}

| 컬럼 | 타입 | 널 | 기본값 | 설명 |
| --- | --- | --- | --- | --- |
| {{컬럼명}} | {{타입}} | {{Y/N}} | {{기본값 또는 -}} | {{한 줄 설명}} |
| {{DDL 순서 그대로, 컬럼 수만큼 행 반복}} | | | | |

- PK: {{기본키 컬럼(들)}}
- 인덱스: {{보조 인덱스와 용도 — 없으면 이 줄 삭제}}
- 관계: {{FK 또는 논리적 관계, 예: lot_id → FAB.LOT.id — 없으면 이 줄 삭제}}

---

## 대표 쿼리

{{선택 — 이 테이블을 쓰는 전형적 쿼리 1~2개, 여기부터는 1200자 컷 아래}}

## 마이그레이션 주의

{{선택 — 변경 이력, 함부로 바꾸면 안 되는 컬럼과 이유}}
